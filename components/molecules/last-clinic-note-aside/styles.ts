import { StyleSheet } from "react-native";
import { FONT, COLOR } from "@/constants/THEME";

export const lastClinicNoteAsideStyles = StyleSheet.create({
  _: {},
  container: {
    rowGap: 12,
  },
  content: {
    rowGap: 16,
  },
});
