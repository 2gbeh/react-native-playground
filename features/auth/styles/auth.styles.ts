import { StyleSheet } from "react-native";

export const authStyles = StyleSheet.create({
  container: {
    // backgroundColor: "red",
  },
  heading: {
    color: "#fff",
    marginBottom: 24,
    textAlign: "center",
    fontSize: 18,
    fontWeight: "600",
  },
  content: {
    rowGap: 16,
  },
});
